saya punya component  contact:

import PropTypes from 'prop-types';
import './contact.style.css';
import { Link } from 'react-router-dom';

const Contact = ({ contact }) => (
  <div>
    <Link to={`/contacts/${contact.id}`}>
      <p>{contact.name}</p>
      <p>{contact.phoneNumber}</p>
    </Link>
  </div>
);

Contact.propTypes = {
  contact: PropTypes.shape({
    name: PropTypes.string.isRequired,
    phoneNumber: PropTypes.string.isRequired
  }).isRequired
};

export default Contact;


dipakai di contaclist companent :
import PropTypes from 'prop-types';
import { Outlet } from 'react-router';
import Contact from './Contact';

const ContactList = ({ contacts }) => (
  <>
    {contacts.map((contact) => (
      <Contact contact={contact} key={contact.phoneNumber} />
    ))}
    <Outlet />
  </>
);

ContactList.propTypes = {
  contacts: PropTypes.arrayOf(Contact.propTypes.contact).isRequired
};

export default ContactList;

nah di hompage pake contact juga tapi dibungkus contact list: 
import ContactList from '../../components/ContactList';
import useAxiosGet from '../../hooks/useAxiosGet';

const HomePage = () => {
  const { data, error, isLoading } = useAxiosGet();
  return (
    <div>
      <h3 style={{ color: 'red' }}>{error && error}</h3>
      <h1>Assalamualaikum Axel</h1>
      {isLoading ? <div className="loader" data-testid="loader" /> : <ContactList contacts={data.contacts} />}
    </div>
  );
};

export default HomePage;


