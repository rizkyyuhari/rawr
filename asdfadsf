const TextInput = (props: Props): React.Node => {
  const { required } = props;

  return (
    <Form
      layout="vertical"
    >
      <Form.Item label={`Name ${!required ? '*' : ''}`}>
        <Input />
      </Form.Item>
    </Form>

  );
};
