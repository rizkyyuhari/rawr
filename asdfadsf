const transformed = portfolios.reduce((result, item) => {
  const date = item.date;
  const group = result.find(group => group.date === date);
  if (group) {
    group.tickers.push(item);
  } else {
    result.push({date, tickers: [item]});
  }

  return result;
}, []);
