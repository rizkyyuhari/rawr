const transformed = Object.values(
  portfolios.reduce((result, item) => {
    const date = item.date;

    if (!result[date]) {
      result[date] = {date, tickers: []};
    }
    result[date].tickers.push(item);

    return result;
  }, {}),
);
